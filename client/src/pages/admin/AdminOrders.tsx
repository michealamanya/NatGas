import { Loader2, PackageCheck } from 'lucide-react';
import { useEffect, useState } from 'react';
import { api } from '../../api/client';
type Order={id:string;orderNumber:string;status:string;deliveryAddress:string;district:string;phone:string;createdAt:string;customer:{firstName:string;lastName:string;email:string};items:{id:string;productName:string;quantity:number}[]};
const statuses=['PENDING','CONFIRMED','PREPARING','OUT_FOR_DELIVERY','COMPLETED','CANCELLED'];
export default function AdminOrders(){const[orders,setOrders]=useState<Order[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');const load=()=>{setLoading(true);api<Order[]>('/admin/orders').then(r=>setOrders(r.data??[])).catch(e=>setError(e.message)).finally(()=>setLoading(false));};useEffect(load,[]);const update=async(id:string,status:string)=>{try{await api(`/admin/orders/${id}`,{method:'PUT',body:JSON.stringify({status})});load();}catch(e){setError(e instanceof Error?e.message:'Could not update order');}};return <><div className="admin-page-head"><div><h1 className="admin-page-title">Customer orders</h1><p className="admin-page-sub">Confirm delivery requests and keep customers informed.</p></div></div>{error&&<p className="form-err">{error}</p>}<div className="admin-card jobs-card"><div className="admin-card-header"><h3><PackageCheck size={16}/>Order queue <span>{orders.length}</span></h3></div>{loading?<div className="loading-state"><Loader2 className="spin"/></div>:<div className="data-table-wrap"><table className="data-table"><thead><tr><th>Order</th><th>Customer</th><th>Items</th><th>Delivery</th><th>Status</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td><b>{o.orderNumber}</b><small>{new Date(o.createdAt).toLocaleString()}</small></td><td><b>{o.customer.firstName} {o.customer.lastName}</b><small>{o.customer.email}<br/>{o.phone}</small></td><td>{o.items.map(i=><div key={i.id}>{i.quantity} × {i.productName}</div>)}</td><td>{o.deliveryAddress}<small>{o.district}</small></td><td><select className="table-select" value={o.status} onChange={e=>update(o.id,e.target.value)}>{statuses.map(s=><option key={s} value={s}>{s.replace(/_/g,' ')}</option>)}</select></td></tr>)}</tbody></table></div>}</div></>}
